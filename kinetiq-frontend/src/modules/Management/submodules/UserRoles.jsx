import React from "react";
import "../styles/UserRoles.css";

const BodyContent = () => {
    return (
        <div className="userol">
            <div className="body-content-container">
                <p>Hello User Roles SubModule!</p>
                <p>Fill this container with your elements, change the display if need be.</p>
                <p>If you're going to style with css, use your unique namespace '.userol' at the start.</p>
            </div>
        </div>

    );
};

export default BodyContent;
